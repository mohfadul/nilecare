# ============================================================
# Flyway Configuration - Payment Gateway Service (Production)
# ============================================================

# Include base configuration
flyway.configFiles=flyway.conf

# Production database
flyway.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/nilecare_payment
flyway.user=payment_service_prod
flyway.password=${DB_PASSWORD_PROD}

# STRICT production settings
flyway.cleanDisabled=true
flyway.cleanOnValidationError=false
flyway.validateOnMigrate=true
flyway.outOfOrder=false

# Placeholders
flyway.placeholders.env=production
flyway.placeholders.db_name=nilecare_payment

# Baseline
flyway.baselineOnMigrate=false

