# ============================================================
# Flyway Configuration - Auth Service (Development)
# ============================================================

# Include base configuration
flyway.configFiles=flyway.conf

# Development database
flyway.url=jdbc:mysql://localhost:3306/nilecare_auth_dev
flyway.user=auth_service_dev
flyway.password=${DB_PASSWORD_DEV}

# Allow clean in development
flyway.cleanDisabled=false

# Placeholders
flyway.placeholders.env=development
flyway.placeholders.db_name=nilecare_auth_dev

# Baseline
flyway.baselineOnMigrate=true

# Target (empty = latest)
flyway.target=

