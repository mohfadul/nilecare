# ============================================================
# Flyway Configuration - Inventory Service
# Database Migration Management
# ============================================================

# Database connection
flyway.url=jdbc:mysql://localhost:3306/nilecare_inventory
flyway.user=${DB_USER}
flyway.password=${DB_PASSWORD}

# Driver
flyway.driver=com.mysql.cj.jdbc.Driver

# Migration locations
flyway.locations=filesystem:./migrations

# Schema history table
flyway.table=schema_version

# Encoding
flyway.encoding=UTF-8

# Validate migrations on migrate
flyway.validateOnMigrate=true

# Placeholders
flyway.placeholders.service_name=inventory_service
flyway.placeholders.env=${ENVIRONMENT}
flyway.placeholders.db_name=nilecare_inventory

# Baseline
flyway.baselineOnMigrate=true
flyway.baselineVersion=0

# Clean (DISABLE IN PRODUCTION!)
flyway.cleanDisabled=true

