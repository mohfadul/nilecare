apiVersion: apps/v1
kind: Deployment
metadata:
  name: device-integration-service
  namespace: nilecare
  labels:
    app: device-integration-service
    tier: integration
spec:
  replicas: 3
  selector:
    matchLabels:
      app: device-integration-service
  template:
    metadata:
      labels:
        app: device-integration-service
        tier: integration
    spec:
      containers:
      - name: device-integration-service
        image: nilecare/device-integration-service:latest
        ports:
        - containerPort: 6003
        env:
        - name: PORT
          value: "6003"
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: nilecare-config
              key: NODE_ENV
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: nilecare-secrets
              key: DATABASE_URL
        - name: MONGODB_URL
          valueFrom:
            secretKeyRef:
              name: nilecare-secrets
              key: MONGODB_URL
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: nilecare-secrets
              key: REDIS_URL
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: nilecare-secrets
              key: JWT_SECRET
        - name: ALLOWED_ORIGINS
          valueFrom:
            configMapKeyRef:
              name: nilecare-config
              key: ALLOWED_ORIGINS
        - name: KAFKA_BROKERS
          valueFrom:
            configMapKeyRef:
              name: nilecare-config
              key: KAFKA_BROKERS
        - name: MQTT_BROKER_URL
          valueFrom:
            configMapKeyRef:
              name: nilecare-config
              key: MQTT_BROKER_URL
        - name: MQTT_USERNAME
          valueFrom:
            secretKeyRef:
              name: nilecare-secrets
              key: MQTT_USERNAME
        - name: MQTT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: nilecare-secrets
              key: MQTT_PASSWORD
        - name: FHIR_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: nilecare-config
              key: FHIR_SERVICE_URL
        - name: EHR_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: nilecare-config
              key: EHR_SERVICE_URL
        resources:
          requests:
            memory: "384Mi"
            cpu: "500m"
          limits:
            memory: "768Mi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 6003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 6003
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: config-volume
          mountPath: /app/config
          readOnly: true
      volumes:
      - name: config-volume
        configMap:
          name: nilecare-config
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: device-integration-service
  namespace: nilecare
  labels:
    app: device-integration-service
    tier: integration
spec:
  selector:
    app: device-integration-service
  ports:
  - port: 6003
    targetPort: 6003
    protocol: TCP
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: device-integration-service-hpa
  namespace: nilecare
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: device-integration-service
  minReplicas: 3
  maxReplicas: 15
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
